# Vue3 Admin Template

一个基于 Vue 3、Vite、TypeScript、Element Plus 和 TailwindCSS 构建的现代化后台管理模板。

## ✨ 特性

- **最新技术栈**: 采用 Vue 3.5, Vite 7, TypeScript 5.9 等最新前沿技术。
- **UI 组件库**: 集成 Element Plus，提供丰富的企业级组件。
- **原子化 CSS**: 内置 TailwindCSS，高效构建样式。
- **状态管理**: 使用 Pinia 进行直观、类型安全的全局状态管理。
- **类型安全**: 全面的 TypeScript 支持，提升代码质量和开发体验。
- **代码规范**: 配置 ESLint (Antfu config), Husky 和 Lint-staged，确保代码风格统一。
- **多种布局**: 提供多种布局模式（左侧菜单、顶部菜单、混合模式等）。
- **常用功能**: 内置路由权限控制、动态主题、全屏、国际化基础等常用功能。

## 📦 环境准备

- Node.js >= 18.0.0
- pnpm >= 8.0.0

## 🚀 快速开始

### 1. 安装依赖

本项目使用 [pnpm](https://pnpm.io/) 作为包管理器。

```bash
pnpm install
```

### 2. 启动开发服务器

```bash
pnpm dev
```

启动后访问: `http://localhost:3333` (默认端口)

### 3. 构建生产环境

```bash
# 构建生产环境
pnpm build:prod

# 构建预发布环境
pnpm build:pre

# 构建测试环境
pnpm build:test
```

### 4. 本地预览构建产物

```bash
pnpm preview
```

### 5. 代码检查与修复

```bash
pnpm lint
```

## 📂 目录结构

```text
src/
├── common/             # 通用资源与逻辑
│   ├── apis/           # API 接口定义
│   ├── assets/         # 静态资源（图标、图片、样式）
│   ├── components/     # 全局通用组件
│   ├── composables/    # 通用组合式函数 (Hooks)
│   ├── constants/      # 常量定义
│   └── utils/          # 工具函数
├── http/               # HTTP 请求封装 (Axios)
├── layouts/            # 布局组件
├── pages/              # 页面视图
├── pinia/              # Pinia 状态管理
├── plugins/            # 插件配置 (Element Plus, SVG Icon 等)
├── router/             # 路由配置与守卫
└── types/              # TypeScript 类型定义
```

## 🛠️ 脚本命令说明

| 命令              | 说明                       |
| ----------------- | -------------------------- |
| `pnpm dev`        | 启动开发服务器             |
| `pnpm build`      | 构建项目（默认生产环境）   |
| `pnpm build:prod` | 指定生产环境模式构建       |
| `pnpm preview`    | 预览构建后的产物           |
| `pnpm lint`       | 运行 ESLint 检查并修复代码 |
| `pnpm test`       | 运行单元测试               |
| `pnpm prepare`    | 安装 Husky 钩子            |

## 📄 许可证

MIT License
